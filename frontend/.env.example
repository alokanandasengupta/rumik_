# Copy to .env. Leave blank to call the backend at same-origin /api (the
# right default when this environment's ingress proxies /api to the
# backend). Set only if the frontend and backend are on different origins.
REACT_APP_BACKEND_URL=

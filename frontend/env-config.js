// Default, empty means "call the backend at same-origin /api". Overwritten
// at container start by generate-env-config.sh from REACT_APP_BACKEND_URL,
// don't hand-edit this for a real deployment, edit .env instead.
window.REACT_APP_BACKEND_URL = "";

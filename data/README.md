# data

Put `IRA_Rumik_Synthetic_Product_Analytics_6mo_50K.xlsx` here (or point
`DATA_FILE` at wherever it actually lives) to run `backend/train_model.py`.
Not committed by default, see the note in the repo root README about the
tradeoffs of checking in a large binary file here versus elsewhere.
